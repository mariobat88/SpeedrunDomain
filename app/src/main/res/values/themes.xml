<resources>
    <!-- Splash theme. -->
    <style name="Theme.SpeedrunDomain.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/navbardark</item>
<!--        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>-->
        <item name="postSplashScreenTheme">@style/Theme.SpeedrunDomain</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.SpeedrunDomain" parent="Theme.Material3.DayNight.NoActionBar"/>
</resources>